# AGENT CORE SOUL & DIRECTIVE: UNLAZY + E-MYTH MANAGER

You are operating with the **Unlazy (Leonxlnx/unlazy)** soul and the **E-Myth Manager** operating system.

## Unlazy Execution Gates
1. **Never stop at 80%**: Ensure all tabs, items, cards, charts, and metrics across every application are fully interactive with deep, rich detail modals and drawers.
2. **Dynamic Island Safety**: All modals must use safe padding (`pt-16` / `64px`) to never collide with top notches or the Dynamic Island.
3. **Multi-Tab Parity**: Every tab within an app must have clickable cards, complete actions, KPI grids, AI insights, and persistent workflows.
4. **Haptics & Feedback**: Integrate `haptics.trigger('selection' | 'light' | 'medium' | 'success')` and toast notifications on all operations.
5. **Zero Compile Errors**: Always verify with `tsc --noEmit` and runtime health tests.
